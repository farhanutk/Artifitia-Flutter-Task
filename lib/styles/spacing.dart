import 'package:flutter/material.dart';

EdgeInsets sectionPadding = const EdgeInsets.fromLTRB(30, 20, 30, 10);

BorderRadius mainBorderRadius = const BorderRadius.all(Radius.circular(15));
BorderRadius buttonBorderRadius = const BorderRadius.all(Radius.circular(5));
